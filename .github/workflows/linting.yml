name: 🧹 Linting

on:
  push:
    branches: [ dev, main ]
  pull_request:
    branches: [ dev, main ]
  workflow_dispatch:   # manual trigger for feature branches

jobs:
  ruff:
    name: Ruff Linter
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v5
      - name: 🧹 Run Ruff
        uses: chartboost/ruff-action@v1

  # mypy:
  #   name: Mypy Type Check
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: 📥 Checkout repository
  #       uses: actions/checkout@v5
  #     - name: 🔍 Read Python version from environment.yml
  #       id: python_version
  #       run: |
  #         PYTHON_VERSION=$(grep '^- python=' ./requirements/environment.yml | cut -d'=' -f2 | xargs)
  #         echo "PYTHON_VERSION=$PYTHON_VERSION" >> $GITHUB_ENV
  #     - name: 🔍 Run Mypy
  #       uses: jpetrucciani/mypy-check@1.14.1
  #       with:
  #         path: '.'
  #         mypy_flags: '--verbose --ignore-missing-imports'
  #         python_version: ${{ env.PYTHON_VERSION }}